[Unit]
Description=Check for pending PhotoBooth offline updates
After=local-fs.target photobooth-persistence.service
Before=display-manager.service

[Service]
Type=oneshot
ExecStart=/usr/local/sbin/photobooth-update --boot-check
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
